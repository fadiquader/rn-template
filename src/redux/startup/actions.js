import { createAction } from 'redux-act';

export const startup = createAction('startup');
export const login = createAction('login');
export const launch = createAction('launch');
export const signout = createAction('signout');
